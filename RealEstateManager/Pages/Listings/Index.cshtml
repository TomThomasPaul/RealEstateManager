@page
@model RealEstateManager.Pages.Listings.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Listing[0].ListingName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Listing[0].ListingPrice)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Listing[0].IsAvailable)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Listing[0].PostedDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Listing[0].AreaSqFt)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Listing) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ListingName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ListingPrice)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsAvailable)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PostedDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AreaSqFt)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
